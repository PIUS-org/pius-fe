import { PageHeader } from '@/shared/ui/page-header';

/** 실제 목록은 #8 에서 붙인다. */
export default function ClientsPage() {
  return (
    <PageHeader kicker="거래처관리" title="거래처 목록" description="화면은 #8 에서 구현합니다." />
  );
}
