import { ClientCreateForm } from '@/features/client/components/client-create-form';

export const metadata = { title: '거래처 등록 · PiUS' };

export default function ClientCreatePage() {
  return <ClientCreateForm />;
}
