import { ProjectCreateForm } from '@/features/project/components/project-create-form';

export const metadata = { title: '프로젝트 개설 · PiUS' };

export default function ProjectCreatePage() {
  return <ProjectCreateForm />;
}
