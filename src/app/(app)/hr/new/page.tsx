import { PersonCreateForm } from '@/features/hr/components/person-create-form';

export const metadata = { title: '인사 등록 · PiUS' };

export default function PersonCreatePage() {
  return <PersonCreateForm />;
}
