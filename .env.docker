# 로컬 개발 — docker compose 로 띄운 백엔드를 사용한다.
#   npm run dev:docker
NEXT_PUBLIC_APP_ENV=docker
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api/v1

# BFF Route Handler 가 서버에서 호출할 주소.
# 브라우저가 아니라 Next 서버가 쓰므로 컨테이너 내부 주소를 넣을 수도 있다.
API_INTERNAL_URL=http://localhost:8080/api/v1
